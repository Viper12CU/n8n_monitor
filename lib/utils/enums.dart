enum Status { avalible, unavalible }

enum LoaderVariant { light, dark }

enum InfoContainerSmVariant { primary, secondary }

enum SnackbarType { info, error, success}

enum WorkflowFilterType { all, active, inactive }

enum ExecutionFilterType { all, canceled, error, running, success, waiting }