import 'package:flutter/material.dart';

class LastAuditTemplate extends StatelessWidget {
  const LastAuditTemplate({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Last audit"));
  }
}
